PRD Mode → Dev-Team output mode. Discovery Mode was the honest route (the request is solution-shaped and under-specified), but the session is unattended and Discovery requires conversation — so I ran the discovery agenda on paper, recorded a Synthesis Checkpoint plus a five-item assumption register in place of the questions I'd have asked, then generated the PRD. Dev-Team was chosen over Stakeholder/AI-Build on evidence rather than default: the fixture this feature extends is itself labelled "PRD — Weekly Project Digest Email (Dev-Team)", same product and same engineering audience, so this is an extension spec for that team (conversion delta to AI-Build is in Appendix C).
