# Archived: asymmetric fixture contamination

Side B located and read `evals/substitution-bakeoff/fixtures/digest-spec-dev-team.md`
— a fixture belonging to eval 3 — and anchored its PRD to the real problem data in it
(40 tickets/month, 2.1 vs 4.4 logins, the `digest_send_log` schema).
Side A never found it (0 fixture markers vs B's 3).

That hands B an evidence base A did not have, from filesystem access rather than skill
quality. A comparator would reward B for being "grounded in real numbers" and score the
wrong variable. Both sides re-run in ../eval-4 with an explicit instruction not to read
anything under evals/.

Kept for the audit trail. Do NOT score these.
